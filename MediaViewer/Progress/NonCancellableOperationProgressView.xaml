<Window x:Class="MediaViewer.Progress.NonCancellableOperationProgressView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{Binding WindowTitle}" Icon="{Binding WindowIcon}" Height="12" Width="300" WindowStyle="None" WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid>
            <ProgressBar HorizontalAlignment="Stretch" Height="10" Maximum="{Binding TotalProgressMax}" Value="{Binding TotalProgress}" VerticalAlignment="Center"/>
        </Grid>
    </Grid>
</Window>
