<UserControl x:Class="MediaViewer.MetaData.TagEditorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" d:DesignWidth="300" d:DesignHeight="135.184">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/MediaViewer;component/Resources/Dictionary.xaml"/>
            </ResourceDictionary.MergedDictionaries>
            <Style TargetType="{x:Type ListBox}">
                <Style.Resources>
                    <SolidColorBrush x:Key="{x:Static SystemColors.ControlBrushKey}" Color="Transparent"/>
                    <SolidColorBrush x:Key="{x:Static SystemColors.WindowBrushKey}" Color="Transparent"/>
                </Style.Resources>
                <Style.Triggers>
                    <DataTrigger Binding="{Binding RelativeSource={RelativeSource Self}, Path=IsEnabled}" Value="true">
                        <Setter Property="Background" Value="White"/>
                    </DataTrigger>
                    <DataTrigger Binding="{Binding RelativeSource={RelativeSource Self}, Path=IsEnabled}" Value="false">
                        <Setter Property="Background" Value="{StaticResource imageGridBackgroundColorBrush}"/>
                    </DataTrigger>
                </Style.Triggers>
            </Style>
        </ResourceDictionary>
    </UserControl.Resources>
    <Grid Margin="0,0,0,10.333">
        <ListBox Name="tagListBox" HorizontalAlignment="Stretch" Height="111" Margin="0,21,0,0" VerticalAlignment="Top" SelectionMode="Multiple"/>
        <Button Content="Add" HorizontalAlignment="Right" Width="50" Margin="0,0,50,0" VerticalAlignment="Top" Click="addButton_Click"
                ToolTip="Add Tag"/>
        <Button Content="Remove" HorizontalAlignment="Right" Width="50" Margin="0,0,0,0" VerticalAlignment="Top" Click="removeButton_Click"
                ToolTip="Remove Selected Tags"/>
        <TextBox Name="addTagTextbox" HorizontalAlignment="Stretch" Margin="0,0,100,0" TextWrapping="Wrap" VerticalAlignment="Top" PreviewKeyDown="addTagTextBox_PreviewKeyDown" />
    </Grid>
</UserControl>
