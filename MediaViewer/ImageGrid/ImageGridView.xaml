<UserControl
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             x:Name="imageGrid" x:Class="MediaViewer.ImageGrid.ImageGridView" 
             xmlns:db="clr-namespace:MediaViewer.DirectoryBrowser"
             xmlns:local="clr-namespace:MediaViewer.MediaFileModel.Watcher"
             xmlns:effects="clr-namespace:MediaViewer.Effects"
             xmlns:ImageGrid="clr-namespace:MediaViewer.ImageGrid"          
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/MediaViewer;component/Resources/Dictionary.xaml"/>
            </ResourceDictionary.MergedDictionaries>
            <effects:GrayscaleEffect x:Key="grayscaleEffect"/>
            <ImageGrid:ThumbnailSelectorConverter x:Key="thumbnailSelector"/>
        </ResourceDictionary>
    </UserControl.Resources>
    <ItemsControl x:Name="itemsControl" ItemsSource="{Binding MediaPage}">
        <ItemsControl.ItemsPanel>
            <ItemsPanelTemplate>
                <UniformGrid x:Name="mainGrid" Columns="5" Rows="5" />
            </ItemsPanelTemplate>
        </ItemsControl.ItemsPanel>
        <ItemsControl.ItemTemplate>
            <DataTemplate>
                <ImageGrid:ImageGridItemView MediaFileItem="{Binding}" Padding="2">
                    <ImageGrid:ImageGridItemView.ContextMenu>
                        <ContextMenu>
                            <MenuItem Header="View" Tag="{Binding Location}" Click="viewMenuItem_Click"/>
                            <Separator/>
                            <MenuItem Header="Select All" Click="selectAllMenuItem_Click"/>
                            <MenuItem Header="Deselect All" Click="deselectAllMenuItem_Click"/>
                            <Separator/>
                            <MenuItem Header="Browse Location" Tag="{Binding Location}" Click="browseMenuItem_Click"/>
                            <MenuItem Header="Open Location" Tag="{Binding Location}" Click="openInExplorerMenuItem_Click"/>
                        </ContextMenu>
                    </ImageGrid:ImageGridItemView.ContextMenu>
                </ImageGrid:ImageGridItemView>
            </DataTemplate>
        </ItemsControl.ItemTemplate>
    </ItemsControl>
</UserControl>
