<UserControl x:Class="MediaViewer.Model.Settings.SettingsNavigationItemView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <Button Grid.Column="0" x:Name="navigationButton" HorizontalAlignment="Center" VerticalAlignment="Center" Width="16" Height="16" Click="navigationButton_Click" Style="{DynamicResource {x:Static ToolBar.ButtonStyleKey}}"
                ToolTip="Settings">
            <Image Source="/MediaViewer;component/Resources/Icons/settings.ico" />
        </Button>
        <Separator Grid.Column="1" Style="{StaticResource {x:Static ToolBar.SeparatorStyleKey}}"/>
    </Grid>
</UserControl>
