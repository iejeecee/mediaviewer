<UserControl
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:MediaPreview="clr-namespace:MediaViewer.MediaPreview" x:Name="imageGrid" x:Class="MediaViewer.ImageGrid.ImageGridView" 
             xmlns:db="clr-namespace:MediaViewer.DirectoryBrowser"
             xmlns:local="clr-namespace:MediaViewer.ImageGrid"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/MediaViewer;component/Resources/Dictionary.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>
    <ItemsControl x:Name="itemsControl" ItemsSource="{Binding Media}">
        <ItemsControl.ItemsPanel>
            <ItemsPanelTemplate>
                <UniformGrid x:Name="mainGrid" Columns="5" Rows="5" />
            </ItemsPanelTemplate>
        </ItemsControl.ItemsPanel>
        <ItemsControl.ItemTemplate>
            <DataTemplate>
                <ToggleButton Name="toggleButton" IsChecked="{Binding IsSelected, Mode=TwoWay}" Margin="2" 
                              IsEnabled="{Binding Path=ItemState, Converter={StaticResource negatedEnumToBooleanConverter}, ConverterParameter={x:Static local:ImageGridItemState.LOADING}}">
                    <ToggleButton.Style>
                        <Style>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsSelected}" Value="true">
                                    <Setter Property="ToggleButton.Background" Value="{StaticResource imageGridSelectedColorBrush}" />
                                </DataTrigger>
                                <DataTrigger Binding="{Binding IsSelected}" Value="false">
                                    <Setter Property="ToggleButton.Background" Value="{StaticResource imageGridBackgroundColorBrush}" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </ToggleButton.Style>
                    <Grid HorizontalAlignment="Stretch" VerticalAlignment="Stretch" ToolTip="{Binding Media.DefaultFormatCaption}" 
                          Tag="{Binding}">
                        <Grid.RowDefinitions>
                            <RowDefinition MinHeight="{Binding ElementName=toggleButton,Path=ActualHeight,Converter={StaticResource mathConverter},ConverterParameter=(x*3)/4}"/>
                            <RowDefinition MinHeight="{Binding ElementName=toggleButton,Path=ActualHeight,Converter={StaticResource mathConverter},ConverterParameter=x/4}"/>
                        </Grid.RowDefinitions>                   
                        <Image HorizontalAlignment="Center" VerticalAlignment="Center" Grid.Row="0">
                            <Image.Style>
                                <Style>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding ItemState}" Value="LOADING">
                                            <Setter Property="Image.Stretch" Value="None"/>                                  
                                            <Setter Property="Image.Source" Value="{StaticResource loadingImage}" />
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding ItemState}" Value="ERROR">
                                            <Setter Property="Image.Stretch" Value="None"/>
                                            <Setter Property="Image.Source" Value="{StaticResource errorImage}" />
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding ItemState}" Value="LOADED">
                                            <Setter Property="Image.Stretch" Value="Uniform"/>                                      
                                            <Setter Property="Image.Source" Value="{Binding Media.Thumbnail}" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Image.Style>
                        </Image>                                       
                        <TextBlock HorizontalAlignment="Center" VerticalAlignment="Center" Text="{Binding Location, Converter={StaticResource pathToFileNameConverter}}" Grid.Row="1"/>                      
                        <Grid.ContextMenu>
                            <ContextMenu>
                                <MenuItem Name="vieMenuItem" Header="View" Tag="{Binding Location}" Click="viewMenuItem_Click"/>
                                <Separator/>
                                <MenuItem Name="selectAllMenuItem" Header="Select All" Click="selectAllMenuItem_Click"/>
                                <MenuItem Name="deselectAllMenuItem" Header="Deselect All" Click="deselectAllMenuItem_Click"/>
                            </ContextMenu>
                        </Grid.ContextMenu>
                    </Grid>
                </ToggleButton>
            </DataTemplate>
        </ItemsControl.ItemTemplate>
    </ItemsControl>
</UserControl>
